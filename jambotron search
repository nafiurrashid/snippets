<!-- Search Start -->

<div class="jumbotron border shadow">
    <div class="row">
        <div class="col-md-8 offset-2">
            <form>
                <div class="input-group">
                    <input name="q" type="text" placeholder="Search Place" class="form-control form-control-lg" />
                    <div class="input-group-append">
                        <button class="btn btn-dark btn-lg">Search</button>
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>
<!-- End Search -->

<!-- View  -->
<!--
def home(request):
    if 'q' in request.GET:
        q=request.GET['q']
        
        context={
        'posts':post.objects.filter(title__icontains=q).order_by('date_posted') | 
        post.objects.filter(content__icontains=q).order_by('-date_posted'),
        
        'news': news.objects.all()
    }
    else:
        
        context={
        'posts': post.objects.all().order_by('-date_posted'),
        'news': news.objects.all(),
        'trvelplan': travelplan_model.object.all()
    }

    return render(request,'blog/home.html', context)
-->
